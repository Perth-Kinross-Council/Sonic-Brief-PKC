{
  "auth": {
    "identityProviders": {
      "azureActiveDirectory": {
        "enabled": true,
        "registration": {
          "openIdIssuer": "${OPENID_ISSUER}",
          "clientIdSettingName": "FRONTEND_CLIENT_ID",
          "clientSecretSettingName": "IGNORED"
        }
      }
    }
  },
   "routes": [
    {
      "route": "/login",
      "allowedRoles": ["anonymous"],
      "rewrite": "/index.html"
    },
    {
      "route": "/*",
      "allowedRoles": ["authenticated"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/static/*",
      "/images/*",
      "/*.css",
      "/*.js"
    ]
  }
}
